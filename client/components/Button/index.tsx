import React from 'react';
import 'twin.macro';

const Button = () => {
  return (
    <button tw="bg-green-400 hover:bg-green-300 px-3 py-2 rounded-sm">Makan Bang</button>
  );
};

export default Button;
